name: todo_app
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.18.5 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Visual Elements
  cupertino_icons: ^1.0.2
  animate_do: ^3.0.2
  shimmer_animation: ^2.1.0+1
  async_button: ^1.0.1-beta.2
  flutter_slidable: ^2.0.0

  # Local Storage
  flutter_secure_storage: ^6.1.0

  # State Management
  provider: ^6.0.4

  # Localizations
  flutter_localizations:
    sdk: flutter

  # Format
  intl: ^0.17.0

  # Firebase
  firebase_core: ^2.3.0
  firebase_auth: ^4.2.0
  cloud_firestore: ^4.2.0

  # UUID
  uuid: ^3.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  generate: true
  uses-material-design: true